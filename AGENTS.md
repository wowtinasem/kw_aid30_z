# AGENTS.md

계원예술대학교 AID+30 작품 전시관 — 에이전트 작업 규칙

**상세 명세는 `AID30-exhibition-spec.md`에 있다. 작업 시작 전에 전체를 읽는다.**

## 이 저장소가 무엇인가

AID+30 수강생의 수업 결과물을 전시하는 정적 웹사이트 한 페이지.
작품마다 배포 링크 · 유튜브 영상 · 기타 자료 세 개의 링크를 가지고, 기수(회차)로 필터한다.

```
index.html        HTML + CSS + JS 전부
data/items.js     SITE / COHORTS / ITEMS — 데이터는 여기에만
img/              썸네일, 로고, og.png
```

## 하지 말 것

- 프레임워크·번들러·패키지 매니저 도입 (`npm install`이 필요하면 설계가 틀린 것)
- 외부 CSS/JS 라이브러리, 웹폰트, 아이콘 폰트 로드
- `index.html` 안에 작품 데이터 정의
- `localStorage` 사용
- 파일·폴더 이름에 한글·공백
- 확정되지 않은 값(브랜드 색, 로고, 기수 수) 임의 결정 — 명세 부록 B 참고, TODO 주석으로 남긴다
- 요청 범위를 넘어선 기능 추가. 스크롤 애니메이션·파티클·테마 토글을 스스로 넣지 않는다

## 지킬 것

- 명세 11절의 회차 순서대로 구현하고 회차마다 커밋한다 (`feat: 4회차 썸네일 규칙`)
- 한 번에 한 회차만. 끝나면 **바꾼 파일과 확인 방법**을 알린다
- 색은 `:root` 변수로만 쓴다. 하드코딩된 HEX를 남기지 않는다
- 브랜드 색 면적은 10% 이하. 갤러리 영역은 흰 벽으로 둔다
- 모든 이미지에 `onerror` 대체 경로가 있어야 한다 (유튜브 썸네일은 외부 자원이다)
- 링크가 없는 버튼은 숨기지 말고 비활성 + 안내 문구
- 키보드만으로 전체 조작이 가능해야 한다. `:focus-visible`을 지우지 않는다
- `prefers-reduced-motion: reduce`를 존중한다
- 문구는 눌렀을 때 일어나는 일을 그대로 쓴다 (`확인` 아님, `앱 열기`)

## 확인 방법

```bash
python3 -m http.server 8000   # 파일 더블클릭으로 열면 data/items.js 로드가 막힌다
```

360px 폭 / 키보드만 / 콘솔 오류 0건 — 이 셋은 매 회차 확인한다.

## 개인정보

이 저장소의 데이터에는 실명·소속·외부 링크가 들어간다.
작품 데이터를 다룰 때 이메일·전화번호·API 키가 섞여 들어가지 않았는지 확인하고, 발견하면 **커밋하지 말고 알린다.**
