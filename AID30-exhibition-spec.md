# AID+30 작품 전시관 — 개발 지침서 v1.0

계원예술대학교 AID+30 · 작성 2026-08-20
구현 도구 Antigravity + Claude Code · 참고 형식 `exhibition-template.html`(부여교육지원청 전시관)

> **에이전트에게**: 이 문서 전체를 먼저 읽고 11절의 회차 순서대로 구현한다. 한 번에 전체를 만들지 않는다.
> 확정되지 않은 값은 부록 B에 모아 두었다. **임의로 지어내지 말고 TODO 주석으로 남긴다.**

---

## 0. 한 문단 요약

계원예술대학교 AID+30 과정 수강생이 수업에서 만든 결과물을 한 페이지에 전시하는 정적 웹사이트다.
작품 하나당 **배포 링크 · 유튜브 시연 영상 · 기타 자료** 세 개의 링크를 가지며, 갤러리 카드에는 썸네일이 보인다.
필터 축은 **기수(회차)** 하나뿐이다. 빌드 도구 없이 `index.html` 한 파일로 동작한다.

---

## 1. 목적과 관객

| 관객 | 이 페이지에서 얻어야 할 것 |
| --- | --- |
| 수강생 본인 | 내 작품이 남의 화면에서 열린다는 확인, 공유할 주소 |
| 다음 기수 지원자 | "나도 할 수 있겠다"는 판단 근거 — 결과물과 **만드는 방법** |
| 학교·사업 관계자 | 성과 확인, 보고자료로 캡처 가능한 화면 |

**설계 원칙 세 줄**

1. 주인공은 작품이다. 페이지가 작품보다 화려하면 실패다.
2. 결과물만 보여 주지 않는다. **어떻게 시켰는지(요청문·PRD)**를 함께 보여 주는 것이 이 전시관의 차별점이다.
3. 링크는 죽는다. 앱이 사라져도 페이지는 성립해야 한다.

---

## 2. 산출물과 기술 제약

**산출물**

```
/
├─ index.html          단일 파일 (HTML + CSS + JS)
├─ data/items.js       작품 데이터 (index.html에서 <script src>로 로드)
├─ img/                썸네일 캡처, 로고, og.png
└─ README.md           운영자용 — 작품 추가하는 법
```

**제약**

- 프레임워크·번들러·패키지 매니저를 쓰지 않는다. `npm install`이 필요한 순간 설계가 틀린 것이다.
- 외부 CSS/JS 라이브러리를 불러오지 않는다. 아이콘은 인라인 SVG.
- 웹폰트를 불러오지 않는다. 시스템 폰트만 쓴다(5절).
- **예외적으로 외부에서 오는 것은 유튜브 썸네일 이미지 하나뿐이다.** 로드 실패 시 대체 카드로 떨어져야 한다(7절).
- 파일·폴더 이름에 한글과 공백을 쓰지 않는다.
- `localStorage`를 쓰지 않는다. 저장할 상태가 없다.

**데이터를 별도 파일로 빼는 이유**: 작품이 기수마다 30~40개씩 늘어난다. `index.html` 안에 두면 작품 하나를 추가할 때마다 AI가 전체 파일을 다시 쓰게 되고, 그 과정에서 기능이 사라진다. 데이터와 화면을 분리한다.

---

## 3. 참고 템플릿과의 관계

`exhibition-template.html`은 **구조 참고용**이다. 코드를 복사하지 말고 아래 표대로 판단한다.

| 가져올 것 | 이유 |
| --- | --- |
| 데이터 3구역 분리(사이트 정보 / 필터 / 작품) | 운영자가 코드를 모르고도 작품을 추가할 수 있다 |
| 카드 그리드 → 도록형 모달 흐름 | 전시 도록의 관습이고 이해가 빠르다 |
| 모달의 Esc 닫기·포커스 트랩·포커스 복원 | 접근성 요건을 이미 만족한다 |
| 링크 없는 작품의 "준비 중" 처리 | 개강 직후 빈 자리를 다루는 유일한 방법 |
| 자동 전시 모드(무조작 → 카드 순환) | 행사장 대형 화면 대응 |

| 버릴 것 | 이유 |
| --- | --- |
| 글래스 연꽃, 디지털 닷 글로브 | 부여(백제) 지역 상징이다. 계원에 붙일 근거가 없다 |
| 남색 계열 색 체계 | 교육청 톤이다. 5절 팔레트로 교체 |
| 부문 색상 칩(초등/중등) | 기수는 성격이 다른 부문이 아니다. 9절 참고 |
| 단일 `url` 필드 | 링크가 세 종류다. 6절 참고 |
| "동작 줄이기 설정 무시" 규칙 | 10절대로 `prefers-reduced-motion`을 존중한다. 행사장 대응은 자동 전시 모드로 충분하다 |

---

## 4. 화면 구조

```
┌────────────────────────────────────────────────┐
│ [계원예술대학교 로고]              AID+30      │  기관 표시줄 (48px, 스크롤 시 유지 안 함)
├────────────────────────────────────────────────┤
│                                                │
│   AID+30 · 2026                                │  eyebrow
│   말로 만든                                    │  키네틱 타이포 (단어별 등장)
│   서른 개의 도구                               │  강조 줄
│                                                │
│   ┌──────────────────────────────┐             │
│   │ ▌"____를 도와주는 도구를..."  │             │  타이핑 카드 — 실제 작품에서 문장 생성
│   └──────────────────────────────┘             │
│                                                │
├────────────────────────────────────────────────┤
│   수강생 30명 · 작품 32개 · 제작 기간 하루      │  소개 스트립 (한 줄, 얇게)
├────────────────────────────────────────────────┤
│   [1기·2026 상반기] [2기·2026 하반기] [전체]    │  기수 필터
│                                                │
│   ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐              │  4열 그리드
│   │16:9 │ │16:9 │ │16:9 │ │16:9 │              │  (태블릿 2열 / 모바일 1열)
│   │ img │ │ img │ │ img │ │ img │
│   ├─────┤ ├─────┤ ├─────┤ ├─────┤
│   │제목 │ │     │ │     │ │     │
│   │소속·이름      │ │     │ │     │
│   │1기·07  ⬚▶⬚  │ │     │ │     │              │  링크 보유 아이콘 3개
│   └─────┘ └─────┘ └─────┘ └─────┘
├────────────────────────────────────────────────┤
│   푸터 — 기관명, 안내 문장, 주소 변경 고지       │
└────────────────────────────────────────────────┘
```

**모달**

```
┌──────────────────────────────────────────┐
│ 1기 · 07호                            ✕  │
│ 도자기 타임머신                          │
│ 이 앱은 ○○과 김○○ 님의 작품입니다.        │
├──────────────────────────────────────────┤
│ [ 썸네일 / 대표 화면 ]                   │
├──────────────────────────────────────────┤
│ 작품 주제   무엇을 하는 도구인지 1~2문장  │
│ 사용 방법   ① ② ③                        │
│ 어떻게 시켰나  실제 요청문 또는 PRD 요약   │  ← 이 칸이 이 전시관의 핵심
├──────────────────────────────────────────┤
│ [앱 열기 ↗] [영상 보기 ▶] [자료 보기]     │  없는 것은 흐리게 비활성
└──────────────────────────────────────────┘
```

---

## 5. 디자인 토큰

**방향** — 갤러리 벽면. 배경은 물러나고 썸네일 30여 장이 각자 다른 색으로 깔린다. 배경이 강하면 작품이 아니라 배경이 보인다. 색은 히어로와 푸터에만 진하게 쓰고, 갤러리 영역은 흰 벽으로 둔다.

```css
:root{
  /* 색 — 브랜드 슬롯은 부록 B 확인 후 교체 */
  --brand:      #16233A;   /* TODO: 계원 공식 색으로 교체. 필터 선택·배지·링크 아이콘 */
  --brand-deep: #0E1726;   /* TODO: brand의 어두운 톤. 히어로 배경·푸터 */
  --brand-tint: #EDF0F5;   /* TODO: brand의 옅은 톤. 라벨 배경 */
  --paper:      #FFFFFF;   /* 카드 */
  --bg:         #F7F7F5;   /* 갤러리 바탕 — 흰 카드가 떠 보이는 최소한의 회색 */
  --ink:        #17171A;
  --muted:      #6E6C68;
  --line:       #E3E1DC;
  --radius:     6px;
}
```

- 브랜드 색은 **면적의 10%를 넘기지 않는다.** 카드·본문에 칠하지 않는다.
- 기수마다 다른 색을 주지 않는다. 기수는 서열도 성격 차이도 아니다.

**타이포** (웹폰트 없음)

```css
--font-display: "Pretendard", "Apple SD Gothic Neo", "Malgun Gothic", sans-serif;
--font-body:    같은 값
--font-mono:    ui-monospace, "SF Mono", Consolas, monospace;
```

- 히어로 제목: `clamp(2.4rem, 6vw, 4.6rem)` / 700 / `letter-spacing:-0.03em` / `line-height:1.1`
- 작품 제목: 1.05rem / 600
- 소속·이름, 배지: 0.82rem / `--muted`
- 본문 최소 16px, 행간 1.7
- **모노는 「어떻게 시켰나」 칸에만 쓴다.** 요청문이 코드가 아니라 "적어 낸 말"이라는 것이 한눈에 보인다.

**모션 — 세 곳에만 쓴다**

1. 히어로 제목 단어별 등장(0.14s 간격)
2. 타이핑 카드
3. 카드 호버 시 2px 상승 + 그림자

그 외 스크롤 애니메이션·패럴랙스·파티클을 넣지 않는다. `prefers-reduced-motion: reduce`에서 1·2를 최종 상태로 즉시 표시한다.

**시그니처 — 히어로 타이핑 카드를 데이터에서 생성한다**

고정 문구를 배열로 넣지 말고, `ITEMS`에서 `prompt` 필드(있으면)나 작품 제목을 뽑아 순환시킨다.
작품이 늘어나면 히어로가 저절로 달라진다. 이 페이지가 "말로 만든 도구들의 전시"라는 주제를 구조로 증명하는 유일한 장치이므로 **다른 장식을 더하지 않는다.**

---

## 6. 데이터 모델

`data/items.js`에 세 개의 전역 상수를 둔다. 다른 파일에서 데이터를 정의하지 않는다.

### SITE

```js
const SITE = {
  orgName:     "계원예술대학교",
  orgLogo:     "img/logo.png",        // "" 이면 텍스트 표시
  programName: "AID+30",

  eyebrow:     "AID+30 작품 전시관",
  titleWords:  ["말로", "만든"],       // 단어별로 떠오름
  titleGlow:   "서른 개의 도구",
  subHTML:     "AID+30 수강생이 수업에서 직접 만든 도구입니다. <b>작품을 누르면</b> 주제와 만든 방법이 열립니다.",

  stats: [                             // 소개 스트립. 3개 고정
    { value: "30", label: "수강생" },
    { value: "32", label: "작품" },
    { value: "하루", label: "제작 기간" },
  ],

  sectionTitle: "작품 목록",
  sectionHint:  "작품을 선택하면 소개 카드가 열립니다",

  badge:      (cohort, n) => `${cohort}기 · ${String(n).padStart(2,"0")}호`,
  artistLine: item => `이 앱은 <b>${item.group} ${item.author}</b> 님의 작품입니다.`,

  labelTopic:  "작품 주제",
  labelUsage:  "사용 방법",
  labelPrompt: "어떻게 시켰나",

  linkText: { app:"앱 열기 ↗", video:"영상 보기 ▶", extra:"자료 보기" },
  linkNone: { app:"앱 주소 준비 중", video:"영상 준비 중", extra:"자료 없음" },

  footerLines: [
    "<span class='org'>계원예술대학교 AID+30</span>",
    "본 전시관은 수강생이 수업 중 제작한 결과물을 소개합니다.",
    "작품 주소는 제작자가 관리하며 변경되거나 종료될 수 있습니다.",
  ],

  idleMs: 40000, showMs: 6500, gapMs: 900,   // 자동 전시 모드
};
```

### COHORTS

```js
const COHORTS = [
  { key: "2", label: "2기", period: "2026 하반기" },   // 배열 첫 항목이 기본 선택
  { key: "1", label: "1기", period: "2026 상반기" },
];
```

- **최신 기수를 배열 맨 앞에 둔다.** 첫 화면 기본 선택은 `COHORTS[0]`이고 "전체"가 아니다.
- 필터 버튼 표기는 `2기 · 2026 하반기`. 기수 번호만 쓰면 2년 뒤에 언제인지 알 수 없다.
- 버튼 오른쪽 끝에 `전체 (64)`를 마지막으로 둔다.

### ITEMS

```js
const ITEMS = [
  {
    no: 7,                              // 기수 안에서의 번호. 기수마다 1부터
    cohort: "1",                        // COHORTS의 key
    group: "○○과",                      // 소속 (학과·팀·회사)
    author: "김○○",
    title: "도자기 타임머신",
    topic: "도자기 사진을 올리면 제작 시기와 기법을 추정해 설명해 주는 도구입니다.",
    usage: "① 사진을 올린다\n② 추정 결과를 확인한다\n③ 설명을 복사해 도록에 붙인다",
    prompt: "도자기 사진을 올리면 시대와 기법을 추정해 주는 웹앱을 만들어 줘.\n결과는 초보자도 알 수 있는 말로 3문장 이내.",
    links: {
      app:   "https://example.vercel.app",
      video: "https://youtu.be/XXXXXXXXXXX",
      extra: ""                          // 없으면 빈 문자열
    },
    image: "img/07-ceramic.png",         // "" 이면 7절 규칙대로 자동 처리
    emoji: "🏺"                          // 최종 대체 카드용
  },
];
```

**필드 규칙**

| 필드 | 필수 | 규칙 |
| --- | --- | --- |
| `no` `cohort` | ● | 두 값의 조합이 고유해야 한다. 중복 시 콘솔에 경고를 찍는다 |
| `title` `topic` | ● | `topic`은 2문장 이내 |
| `usage` | | 줄바꿈 `\n`. 3단계 이내 권장 |
| `prompt` | | **가능한 한 채운다.** 이 전시관의 핵심 콘텐츠다. 비면 모달에서 해당 칸을 통째로 숨긴다 |
| `links` | ● | 세 키가 항상 존재해야 한다. 없으면 `""` |
| `image` | | 비면 7절 우선순위 적용 |
| `emoji` | | 비면 `🧩` |

정렬은 **기수 내림차순 → `no` 오름차순**. 데이터 배열 순서에 의존하지 않는다.

---

## 7. 썸네일 규칙

우선순위대로 시도한다.

1. `item.image`가 있으면 그것
2. 없고 `links.video`가 있으면 **유튜브 자동 썸네일**
3. 둘 다 없으면 이모지 + 브랜드 틴트 배경의 대체 카드

**유튜브 ID 파싱** — 아래 네 형태를 모두 처리한다.

```
https://youtu.be/ID
https://www.youtube.com/watch?v=ID
https://www.youtube.com/shorts/ID
https://www.youtube.com/embed/ID
```

썸네일 주소는 `https://img.youtube.com/vi/{ID}/hqdefault.jpg`를 쓴다.
`maxresdefault.jpg`는 영상에 따라 404가 나므로 쓰지 않는다.

**실패 처리** — `<img>`에 `onerror`를 달아 3번 대체 카드로 떨어뜨린다. 네트워크가 없는 환경에서도 갤러리가 깨지지 않아야 한다.

**비율** — 모든 카드 이미지는 `aspect-ratio: 16/9` + `object-fit: cover`. 유튜브 썸네일이 16:9이므로 여기에 맞춰야 직접 캡처와 섞여도 줄이 흔들리지 않는다.

**영상이 있는 카드**에는 썸네일 위에 재생 삼각형을 얹는다(인라인 SVG, 반투명 원 배경).

---

## 8. 컴포넌트 명세

### 카드

- 표시 요소: 썸네일 / 제목 / `소속 · 이름` / 기수·번호 배지 / 링크 보유 아이콘 3개
- **링크 아이콘 3개**는 보유 여부를 색으로 구분한다. 보유 = `--brand`, 미보유 = `--line`.
  `title` 속성과 `aria-label`에 "배포 링크 있음 / 없음"을 넣는다. **색만으로 구분하지 않는다.**
- 카드 전체가 하나의 `<button>`이다. 카드 안에 링크를 중첩하지 않는다.
- 호버: 2px 상승 + 그림자. 포커스: `:focus-visible` 2px 외곽선.

### 모달

- `<dialog>` 요소를 쓰거나 동등한 접근성을 직접 구현한다.
- Esc 닫기 / 배경 클릭 닫기 / 포커스 트랩 / 닫을 때 원래 카드로 포커스 복원.
- 열릴 때 `body`에 `overflow:hidden`, 닫을 때 해제. **스크롤 위치를 잃지 않게 한다.**
- 링크 버튼은 `target="_blank" rel="noopener noreferrer"`.
- 링크가 없는 버튼은 `<button disabled>`로 두고 `SITE.linkNone` 문구를 표시한다. **숨기지 않는다.**
- `prompt`가 비면 그 칸 전체(라벨 포함)를 렌더하지 않는다.

### 딥링크

모달을 열 때 주소를 `#w-1-07`(기수-번호)로 바꾸고, 그 주소로 접속하면 해당 모달이 열린 상태로 시작한다.
수강생이 **자기 작품을 바로 가리키는 주소**를 공유할 수 있어야 한다. 뒤로가기로 모달이 닫혀야 한다.

### 필터

- 클릭 시 카드를 다시 그린다. URL은 바꾸지 않는다.
- 선택 상태는 `aria-pressed`로 표현한다.
- 결과가 0개인 기수는 버튼을 만들지 않는다.

### 자동 전시 모드

- 무조작 `idleMs` 경과 → **앱 링크가 있는 작품만** 순환하며 모달을 자동으로 열고 닫는다.
- 마우스·키보드·터치·스크롤 중 무엇이든 감지되면 **즉시 해제**하고 열려 있던 모달을 닫는다.
- `prefers-reduced-motion`과 무관하게 동작한다(전환은 페이드만).
- 데이터가 5개 미만이면 자동 전시 모드를 켜지 않는다.

---

## 9. 접근성·성능 기준

| 항목 | 기준 |
| --- | --- |
| 언어 | `<html lang="ko">` |
| 명도 대비 | 본문 4.5:1 이상, 큰 글자 3:1 이상 |
| 포커스 | `:focus-visible` 외곽선을 지우지 않는다 |
| 조작 영역 | 최소 44×44px |
| 제목 단계 | h1 → h2 → h3 순서를 건너뛰지 않는다 |
| 이미지 | 모든 `<img>`에 의미 있는 `alt`(작품 제목) |
| 색 의존 | 링크 보유 여부를 색과 형태·라벨로 함께 표시 |
| 모션 | `prefers-reduced-motion: reduce` 존중 |
| 모바일 | 360px 폭에서 가로 스크롤이 생기지 않는다 |
| 자바스크립트 미실행 | 최소한 안내 문구 한 줄이 보인다(`<noscript>`) |
| 콘솔 | 빨간 오류 0건 |

---

## 10. 문구 작성 기준

- 버튼은 눌렀을 때 일어나는 일을 그대로 쓴다. `확인` 아님, `앱 열기`.
- 빈 상태는 사과하지 않고 다음 행동을 알려 준다. "작품이 없습니다"가 아니라 "이 기수의 작품은 준비 중입니다."
- "간단히", "쉽게", "당연히"를 쓰지 않는다.
- 수강생 호칭은 `님`으로 통일한다(부여 사례의 `선생님` 아님).

---

## 11. 구현 순서

회차마다 커밋하고 브라우저에서 확인한 뒤 다음으로 넘어간다. **한 번에 전체를 요청하지 않는다.**

| 회차 | 요청 범위 | 끝나면 확인할 것 |
| --- | --- | --- |
| 1 | 파일 골격 + `:root` 토큰 + 상단 표시줄 + 푸터 + `data/items.js` 빈 구조 | 색과 글자 크기가 5절대로 나오는가 |
| 2 | 히어로(키네틱 타이포·타이핑 카드) + 소개 스트립 | 단어가 순서대로 뜨고 타이핑이 순환하는가 |
| 3 | `ITEMS` 렌더링 + 4열 그리드 + 반응형(2열/1열) | 샘플 4개가 16:9로 정렬되는가 |
| 4 | 썸네일 우선순위 + 유튜브 ID 파싱 + `onerror` 대체 | 유튜브 링크만 있는 작품에 썸네일이 뜨는가 |
| 5 | 모달 + 링크 버튼 3종 + 비활성 처리 + 접근성(Esc·포커스 트랩·복원) | 키보드만으로 열고 닫히는가 |
| 6 | 기수 필터 + 정렬 + 딥링크(`#w-1-07`) | 기본이 최신 기수인가, 주소 공유가 되는가 |
| 7 | 자동 전시 모드 + 조작 시 해제 | 40초 뒤 순환하고 마우스를 움직이면 멈추는가 |
| 8 | 실제 데이터 투입 + 12절 검증표 전 항목 | 표의 모든 줄이 통과하는가 |
| 9 | OG 메타 + `README.md` + 배포 | 카카오톡에 붙였을 때 미리보기가 뜨는가 |

3회차 이후 요청에는 이 문장을 덧붙인다.

```text
기존 기능과 데이터는 바꾸지 말고 이번에 말한 것만 추가해 줘.
바꾼 파일과 내가 브라우저에서 확인할 방법을 알려 줘.
```

---

## 12. 완료 기준 검증표

| 항목 | 확인 방법 |
| --- | --- |
| 모바일 | 개발자도구 360px에서 가로 스크롤 없음 |
| 키보드 | 마우스 없이 Tab·Enter·Esc만으로 필터·카드·모달·링크 도달 |
| 썸네일 3종 | 직접 이미지 / 유튜브 자동 / 이모지 대체가 각각 정상 |
| 이미지 실패 | 개발자도구에서 이미지 요청 차단 후 대체 카드로 떨어지는지 |
| 링크 없음 | 세 링크가 모두 빈 작품을 하나 넣어 모달이 깨지지 않는지 |
| 필터 | 기본이 최신 기수, 전체 개수가 실제 배열 길이와 일치 |
| 딥링크 | `#w-1-07` 주소로 새로고침 시 해당 모달이 열림, 뒤로가기로 닫힘 |
| 자동 전시 | 40초 대기 후 순환 시작, 마우스 이동 시 즉시 해제 |
| 정렬 | 배열 순서를 뒤섞어도 화면 순서가 유지됨 |
| 콘솔 | 빨간 오류 0건 |
| 보안 | 파일 전체 검색으로 이메일·전화번호·API 키·비밀번호 0건 |
| 용량 | `index.html` 200KB 이하, 썸네일 장당 300KB 이하 |

---

## 13. 개인정보·저작권 점검 — 배포 전 필수

공개 웹이다. 아래를 **작품 데이터 투입 시점에 한 번, 배포 직전에 한 번** 확인한다.

- [ ] 수강생 실명·소속 공개에 대한 동의를 받았는가 (미동의자는 `김○○` 형태 또는 제외)
- [ ] 썸네일 캡처에 **계정 이메일·프로필 사진**이 찍혀 있지 않은가 (브라우저 캡처의 우상단·좌하단)
- [ ] 작품 앱 자체에 실제 고객·학생·거래처 데이터가 들어 있지 않은가
- [ ] 유튜브 영상이 **일부공개(비공개 아님)** 이상으로 설정돼 있는가
- [ ] 기타 자료 링크(구글 시트·문서)의 공유 권한이 "링크가 있는 모든 사용자 — 뷰어"인가, 편집 권한이 열려 있지 않은가
- [ ] 학교 로고 사용 승인을 받았는가
- [ ] 작품에 쓰인 이미지·글꼴의 이용 조건을 확인했는가

**하나라도 미확인이면 해당 작품의 `links`를 비우고 "준비 중"으로 두고 배포한다.** 페이지는 나중에 고칠 수 있지만 공개된 정보는 되돌릴 수 없다.

---

## 14. 배포

- GitHub Pages 또는 Vercel. 저장소는 **Public**(Pages 사용 시), 작품 데이터에 개인정보가 없음을 13절로 확인한 뒤에 공개한다.
- `<head>`에 OG 메타 6줄을 넣는다. `og:image`는 히어로를 1200×630으로 캡처해 `img/og.png`로 둔다.
- 절대 주소가 필요하므로 배포 주소가 정해진 뒤 채운다.

```html
<meta property="og:title" content="AID+30 작품 전시관 — 계원예술대학교">
<meta property="og:description" content="AID+30 수강생이 수업에서 직접 만든 도구 32개">
<meta property="og:image" content="https://{배포주소}/img/og.png">
<meta property="og:url" content="https://{배포주소}/">
<meta property="og:type" content="website">
<meta name="twitter:card" content="summary_large_image">
```

`README.md`에는 **작품 하나를 추가하는 절차**만 적는다. 개발 설명이 아니라 운영자용 문서다.

---

## 15. Antigravity + Claude Code 운용 요령

- 이 문서와 `AGENTS.md`를 저장소 루트에 두고 시작한다. Claude Code는 `AGENTS.md`를 자동으로 참조한다.
- **회차마다 커밋한다.** 커밋 메시지는 `feat: 3회차 갤러리 그리드`처럼 회차 번호를 남긴다. 되돌릴 지점이 된다.
- 확인은 `python3 -m http.server 8000` 후 `localhost:8000`. 파일 더블클릭으로 열면 `data/items.js` 로드가 막힐 수 있다.
- 작품 데이터 추가는 코드 작업이 아니다. **`data/items.js`만 열어서 붙여넣는다.** 이 작업에 AI를 쓰지 않는 편이 안전하다.

**1회차 요청문 예시**

```text
저장소의 AID30-exhibition-spec.md를 읽고 11절 1회차만 구현해 줘.
2절 파일 구성대로 만들고, 5절 :root 토큰을 그대로 쓴다.
브랜드 색은 부록 B가 미확정이므로 임시값과 TODO 주석을 남겨 줘.
2회차 이후 내용은 아직 만들지 말고, 확인할 방법을 알려 줘.
```

---

## 부록 A. 작품 제출 양식 ↔ 데이터 필드

구글폼 항목명을 필드와 1:1로 맞춰 두면 응답 시트를 그대로 배열로 옮길 수 있다.

| 구글폼 질문 | 필드 | 형식 |
| --- | --- | --- |
| 소속(학과·팀) | `group` | 단답 |
| 이름 | `author` | 단답 |
| 작품 이름 | `title` | 단답 |
| 이 도구는 무엇을 해 주나요 (2문장) | `topic` | 장문 |
| 사용 방법 (①②③) | `usage` | 장문 |
| **AI에게 실제로 뭐라고 시켰나요** | `prompt` | 장문 |
| 배포 주소 | `links.app` | URL |
| 시연 영상 주소 | `links.video` | URL |
| 기타 자료 주소 | `links.extra` | URL |
| 대표 화면 캡처 | `image` | 파일 업로드 |
| 이름·소속 공개에 동의합니다 | — | 체크(필수) |

## 부록 B. 미확정 사항

구현 전에 확인해야 하며, **에이전트가 임의로 채우지 않는다.**

| 항목 | 상태 | 필요한 것 |
| --- | --- | --- |
| 계원예술대학교 공식 색상값 | **미확인** | UI 가이드 또는 로고 파일의 HEX. `--brand` 3종에 반영 |
| 학교 로고 이미지 | 미확보 | `img/logo.png` (배경 투명 PNG 또는 SVG) |
| AID+30 정식 명칭·부제 | 미확정 | 상단 표시줄과 히어로 문구 |
| 기수 구성 | 미확정 | 현재 몇 기까지인지, 기수별 작품 수 |
| 히어로 문구 | 초안 | 5절 `titleWords` / `titleGlow`는 예시다. 확정 필요 |
| 통계 숫자 3종 | 초안 | 실제 수강생·작품 수 |
| 배포 주소 | 미정 | OG 메타에 필요 |
