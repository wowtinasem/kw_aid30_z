# AID+30 작품 전시관 — 운영 가이드

계원예술대학교 AID+30 수강생의 수업 결과물을 전시하는 정적 웹사이트입니다.
이 문서는 **작품을 추가·수정하는 분**을 위한 안내입니다. 코드를 몰라도 됩니다.

이 전시관은 **대표님 대상 Z트랙** 과정입니다. 화면 상단의 [바이브코딩] / [영상]
버튼으로 작품과 영상을 오갈 수 있습니다.

```text
index.html        화면·동작 (건드리지 않습니다)
data/items.js     작품·영상 데이터 — 수정은 이 파일에서만 합니다
img/              썸네일 캡처, 로고, og.png
video/            직접 올린 영상 파일 (mp4)
```

## 미리 보기

파일을 더블클릭으로 열면 `data/items.js` 로드가 막힙니다. 간단한 서버로 여세요.

```bash
python -m http.server 8000
```

브라우저에서 <http://localhost:8000> 접속.

## 작품 하나 추가하기

`data/items.js`를 열어 `ITEMS` 배열에 아래 형태의 객체를 붙여 넣고 저장하면 끝입니다.

```js
{
  no: 5, cohort: "z",                  // 트랙 안에서의 번호 — (트랙, 번호) 조합이 겹치면 안 됩니다
  group: "○○컴퍼니", author: "김○○",    // 실명은 공개 동의를 받은 경우에만
  title: "작품 이름",
  topic: "무엇을 하는 도구인지 2문장 이내.",
  usage: "① 첫 단계\n② 둘째 단계",
  prompt: "AI에게 실제로 시킨 말.",      // ★ 이 전시관의 핵심 — 가능한 한 채우세요
  links: {
    app:   "https://...",              // 없으면 "" — 버튼이 '준비 중'으로 표시됩니다
    video: "https://youtu.be/...",     // 유튜브 주소 — 썸네일이 자동으로 걸립니다
    extra: ""
  },
  image: "img/2-04-name.png",          // 대표 화면 캡처. "" 이면 유튜브 썸네일 → 이모지 순 자동 대체
  emoji: "🎨"
},
```

- 구글폼으로 제출을 받는 경우, 폼 항목과 필드의 1:1 대응은
  `AID30-exhibition-spec.md` **부록 A**에 있습니다. 응답 시트를 그대로 옮기면 됩니다.
- 이미지는 **16:9 비율, 장당 300KB 이하** 권장. 파일명은 영문·숫자만 (`img/z-05-name.png`).
- 새 트랙·기수가 생기면 `COHORTS` 배열 **맨 앞**에 추가하세요 (정렬·배지에 사용됩니다).

## 영상 추가하기

[영상] 버튼 화면에 보이는 카드입니다. `data/items.js`의 `VIDEOS` 배열에 추가합니다.

```js
/* 방법 ① 유튜브 링크 — 일반·쇼츠 모두 가능, 썸네일 자동 */
{ title: "영상 이름", group: "○○컴퍼니", author: "김○○",
  youtube: "https://youtube.com/shorts/XXXXXXXXXXX", file: "",
  desc: "한 줄 소개" },

/* 방법 ② 직접 업로드 — mp4를 video/ 폴더에 넣고 경로를 적습니다 */
{ title: "영상 이름", group: "○○컴퍼니", author: "이○○",
  youtube: "", file: "video/clip-01.mp4",
  desc: "한 줄 소개" },
```

- `youtube`와 `file` 중 **하나만** 채우세요 (둘 다 있으면 유튜브 우선)
- 쇼츠(세로) 영상도 재생 화면에서 가운데 정렬로 그대로 나옵니다
- 직접 올리는 mp4는 **50MB 이하** 권장 (GitHub 파일 제한 100MB)

## 올리기 전에 확인 — 개인정보·저작권

공개 웹입니다. 작품을 추가할 때마다 확인하세요 (상세: 명세 13절).

- [ ] 실명·소속 공개 동의를 받았는가 (미동의자는 `김○○` 형태)
- [ ] 캡처 이미지에 계정 이메일·프로필 사진이 찍혀 있지 않은가
- [ ] 유튜브 영상이 일부공개 이상인가, 자료 링크가 "뷰어" 권한인가
- [ ] 이메일·전화번호·API 키가 데이터에 섞이지 않았는가

하나라도 미확인이면 해당 작품의 `links`를 비워 "준비 중"으로 두고 올리세요.

## 배포

GitHub `main` 브랜치에 푸시하면 GitHub Pages가 자동으로 반영합니다 (1~2분 소요).

- 공개 주소: <https://wowtinasem.github.io/kw_aid30_z/>
- 작품별 공유 주소: `https://wowtinasem.github.io/kw_aid30_z/#w-z-01` (트랙-번호) —
  해당 작품의 소개 카드가 열린 상태로 시작합니다

## 아직 남은 것 (명세 부록 B)

| 항목 | 할 일 |
| --- | --- |
| 계원 공식 브랜드 색 | `index.html` 상단 `:root`의 `--brand` 3종 교체 (TODO 주석 표시됨) |
| 학교 로고 | `img/logo.png` 배치 (배경 투명 PNG) — 없으면 텍스트로 표시됩니다 |
| 실제 작품 데이터 | `ITEMS`의 예시 4건을 실제 데이터로 교체 |
| og.png 재캡처 | 브랜드 색 확정 후 히어로를 1200×630으로 다시 캡처 |

## 지원 및 크레딧

이 콘텐츠는 교육부와 국가평생교육진흥원의 '재직자 AI디지털(AID) 집중과정 사업'의
지원을 받아 제작한 교육과정의 산출물입니다.

- **주관·운영**: 계원예술대학교
- **제작·교육**: 마인드캔버스
- **협력**: 경기중소벤처기업협회
- **콘텐츠 제작 참여**: (주)SSMI

---

개발 참고 문서: `AID30-exhibition-spec.md`(명세), `AGENTS.md`(작업 규칙),
`exhibition-template.html`(참고용 원형 템플릿 — 사이트에서는 사용되지 않습니다).
